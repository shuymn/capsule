# capsule — macOS zsh Prompt Engine v1

## Goal

macOS + zsh 専用 Rust 製プロンプトエンジン v1。daemon が prompt の計算・レイアウト・レンダリングを担い、zsh は coproc relay 経由の薄い glue に徹する。

## Constraints

- Target: macOS + zsh (Linux は best-effort fallback)
- Architecture: [docs/architecture.md](docs/architecture.md) 参照
- Runtime: tokio current_thread
- Lint: 既存 Cargo.toml lints を workspace に移行して維持
- v1 で追加しない依存: gix, serde/serde_json, nix, async-trait

## Open Questions

- Question: coproc の read/write fd 番号取得が zsh バージョンにより異なる可能性
  - Class: `risk-bearing`
  - Resolution: `spike` (macOS 同梱 zsh 5.9 で動作確認、Theme 6 実装時)
  - Status: `open`

- Question: CI を macOS runner に変更するか、macOS job を追加するか
  - Class: `risk-bearing`
  - Resolution: `decision`
  - Status: `resolved` — macOS runner に変更。capsule は macOS 専用ツールのため target platform で CI を実行する

- Question: Async trait object の制約 (dyn Trait + async fn)
  - Class: `non-blocking`
  - Resolution: v1 は固定モジュールセット。enum dispatch または static dispatch で回避
  - Status: `resolved`

- Question: spawn_blocking は current_thread runtime でも別スレッドで動作するか
  - Class: `non-blocking`
  - Resolution: する (tokio の blocking thread pool は runtime flavor に依存しない)
  - Status: `resolved`

- Question: `launch_activate_socket` の呼び出し方法 (raw FFI vs crate)
  - Class: `risk-bearing`
  - Resolution: `decision` — raw FFI。API は C 関数 1 つ (`int launch_activate_socket(const char*, int**, size_t*)`)。1 関数のために crate を追加する理由がない
  - Status: `resolved`

- Question: launchd 未設定環境 (開発中、テスト、将来の Linux) でも daemon を起動できるか
  - Class: `blocking`
  - Resolution: `decision` — `launch_activate_socket` 成功 → launchd モード、失敗 → 従来の bind モード。自動検出で分岐し、standalone モードを維持する
  - Status: `resolved`

- Question: launchd plist の `SockPathName` に `$TMPDIR` は使えない (macOS の `$TMPDIR` は再起動ごとに変わり、plist 内で変数展開されない)
  - Class: `blocking`
  - Resolution: `decision` — `~/.capsule/capsule.sock` に変更。`$HOME` は launchd が展開する。macOS 慣例の `~/Library/...` より短く、sun_path 104 bytes 制限にも安全
  - Status: `resolved`

- Question: Config file location convention
  - Class: `blocking`
  - Resolution: `decision` — `$XDG_CONFIG_HOME/capsule/config.toml` (fallback `~/.capsule/config.toml`)。XDG_CONFIG_HOME が設定されていればそれを尊重し、未設定なら socket と同じ `~/.capsule/` に同居
  - Status: `resolved`

- Question: Config reload mechanism (restart-only vs hot-reload)
  - Class: `risk-bearing`
  - Resolution: `decision` — restart-only。config 変更は daemon 再起動で反映。standalone: 次の `capsule connect` が auto-start。launchd: `launchctl kickstart -k` または `capsule daemon reload`。SIGHUP hot-reload は follow-up
  - Status: `resolved`

- Question: Toolchain config precedence (default list との merge/override 方針)
  - Class: `risk-bearing`
  - Resolution: Theme 15 planning 時に決定
  - Status: `open`

- Question: Env var segments の wire protocol 拡張方式
  - Class: `blocking` (env var segment theme のみ。Theme 14/15 には non-blocking)
  - Resolution: Request の reserved meta field 形式と init.zsh の env var 収集方法を決定する必要がある
  - Status: `open`

## Theme Backlog

- [x] Theme 1: Workspace Foundation
  - Outcome: 3-crate workspace がコンパイルし、CLI skeleton が `--help` を返す
  - Goal: workspace 構造 (protocol/core/cli)、共通 lint/deps 定義、CLI サブコマンド骨格、既存 `src/main.rs` 削除、CI macOS 化
  - Must Not Break: `task check` (既存の lint/fmt/build パイプライン)
  - Non-goals: サブコマンドの実装、依存の実使用、テストヘルパー以上のテスト基盤
  - Acceptance (EARS):
    - When `task check` is run, the system shall pass all checks on the workspace
    - When `cargo run -p capsule-cli -- --help` is run, the output shall list `daemon`, `connect`, `init` subcommands
    - When CI runs, all jobs shall execute on macOS runner
  - Evidence: `run=cargo run -p capsule-cli -- --help; oracle=stdout contains "daemon" and "connect" and "init"; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`
  - Executable doc: `cargo run -p capsule-cli -- --help` → 出力に `daemon`, `connect`, `init` が含まれる
  - Why not split vertically further?: crate 骨格と CLI skeleton は相互依存 (cli は core/protocol に依存)。片方だけでは `task check` で検証不能
  - Escalate if: `workspace.lints` の移行で既存 lint 設定の挙動が変わる

- [x] Theme 2: Wire Protocol
  - Outcome: netstring codec と Request/RenderResult/Update のシリアライズが round-trip テストを通る
  - Goal: `capsule-protocol` に netstring encode/decode、message 型 (Request/RenderResult/Update)、SessionId newtype、async codec (MessageReader/MessageWriter) を実装
  - Must Not Break: `task check`
  - Non-goals: 実際の socket I/O、daemon 統合、wire format の拡張性
  - Acceptance (EARS):
    - When arbitrary bytes are encoded then decoded, the system shall return the original bytes
    - When a Request is serialized to wire format then deserialized, the system shall return an equivalent Request
    - When invalid wire input is decoded, the system shall return Err without panicking
    - When messages are written to and read from an async duplex channel, the system shall preserve message content
  - Evidence: `run=cargo test -p capsule-protocol; oracle=all tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-protocol` — netstring round-trip (空/ASCII/UTF-8/長い文字列)、message round-trip (Request/RenderResult/Update)、不正入力エラー、async codec round-trip
  - Why not split vertically further?: netstring・message・codec は 1 つの wire format を構成し、round-trip テストが分離不能
  - Escalate if: wire format の field 順序や encoding 方式で追加の設計判断が必要になった場合

- [x] Theme 3: Module System
  - Outcome: Module trait + 7 モジュール (directory, status, cmd_duration, time, character, git, toolchain) が RenderContext から正しい出力を返す
  - Goal: `capsule-core` に Module trait (sync)、ModuleSpeed enum、RenderContext、ModuleOutput を定義し、全 v1 モジュールを実装。GitProvider trait + CommandGitProvider (std::process::Command)
  - Must Not Break: `task check`
  - Non-goals: ANSI スタイリング (Theme 4)、モジュール出力の合成 (Theme 4)、daemon 統合 (Theme 5)
  - Acceptance (EARS):
    - When cwd is `$HOME`, directory module shall output `~`
    - When last_exit_code is 0, status module shall return None
    - When last_exit_code is non-zero, character module shall output content distinguishable from success case
    - When duration_ms is below 2000, cmd_duration module shall return None
    - When a git repository with staged changes exists, git module shall report staged count > 0
    - When `Cargo.toml` exists in cwd, toolchain module shall output a string containing "rust"
  - Evidence: `run=cargo test -p capsule-core -- module; oracle=all module tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=git tests use tempfile + git init`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- module` — 各モジュールの入出力テスト (temp git repo 使用)
  - Why not split vertically further?: 全モジュールが同一 trait と RenderContext を共有。個別 theme は同一構造の繰り返しになり overhead が勝つ
  - Escalate if: GitProvider trait の設計が `git status --porcelain=v2` のパース結果と整合しない

- [x] Theme 4: Rendering Pipeline
  - Outcome: Module 出力群が left1 (info line) / left2 (input line) prompt 文字列に合成される
  - Goal: style.rs (ANSI + zsh `%{..%}` escape)、layout.rs (display width 計算 + truncation)、Composer (info_left + info_right → right-padded line1、input_left → line2) を実装
  - Must Not Break: `task check`
  - Non-goals: daemon 統合、実際のターミナル出力、カスタマイズ可能な色設定
  - Acceptance (EARS):
    - When module outputs fit within terminal width, the info line shall right-align right-side modules with space padding
    - When module outputs exceed terminal width, directory shall be truncated before right-side modules are dropped
    - When ANSI escape sequences are present in input, display width calculation shall exclude them
    - When zsh `%{..%}` wrappers are present in input, display width calculation shall exclude them
  - Evidence: `run=cargo test -p capsule-core -- render; oracle=all render tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- render` — width 計算 (ANSI/CJK/zsh escape)、padding、truncation、full composition
  - Why not split vertically further?: style → layout → composition は 1 パイプライン。分離すると合成テストが成立しない
  - Escalate if: CJK 文字の width 計算が unicode-width crate で不正確な場合

- [x] Theme 5: Daemon Server
  - Outcome: Unix socket で listen する daemon が request を受けて fast module の RenderResult を即時返し、slow module 完了後に Update を送信する
  - Goal: socket lifecycle (bind/accept/shutdown)、Session + SessionMap、BoundedCache (TTL + size limit)、request pipeline (fast → cache check → respond → slow recompute → update) を実装
  - Must Not Break: `task check`
  - Non-goals: CLI サブコマンド統合 (Theme 6)、zsh glue (Theme 6)、構造化ログ出力 (Theme 7)
  - Acceptance (EARS):
    - When a client connects and sends a valid Request, the daemon shall respond with a RenderResult containing fast module outputs
    - When slow modules complete with results different from the initial response, the daemon shall send an Update message
    - When a stale socket file exists and no daemon is listening, the daemon shall remove it and bind successfully
    - When SIGTERM is received, the daemon shall shut down gracefully and remove the socket file
    - When request generation is not greater than session's last generation, the daemon shall discard the request
    - When cache contains fresh slow module results for the same cwd, the daemon shall use cached values
  - Evidence: `run=cargo test -p capsule-core -- daemon; oracle=all daemon tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=temp socket + MockGitProvider for deterministic slow module`
  - Gates: `static`, `integration`, `system`
  - Executable doc: `cargo test -p capsule-core -- daemon` — temp socket daemon 起動 → 接続 → request → RenderResult 検証 → slow module 完了 → Update 検証 → SIGTERM → socket cleanup
  - Why not split vertically further?: session・cache・handler は request pipeline の構成要素。pipeline 統合なしに daemon の正しさを検証できない
  - Escalate if: tokio current_thread + spawn_blocking の組み合わせで想定外の挙動が発生

- [x] Theme 6: CLI + zsh Integration
  - Outcome: `eval "$(capsule init zsh)"` で zsh session に prompt が表示され、`capsule connect` が daemon との coproc relay を提供する
  - Goal: `capsule daemon` / `capsule connect` / `capsule init zsh` サブコマンド実装、zsh glue スクリプト生成 (precmd/preexec/update handler/fallback/coproc 監視)、coproc relay (stdin/stdout ↔ Unix socket)、daemon 自動起動
  - Must Not Break: `task check`、ユーザーの既存 zsh 設定 (ZDOTDIR 隔離で保証)
  - Non-goals: launchd 統合、設定ファイル、テーマカスタマイズ、Linux 対応
  - Acceptance (EARS):
    - When `capsule init zsh` is run, the output shall be valid zsh script containing `_capsule_` prefixed functions
    - When the zsh glue is eval'd in a clean zsh session (ZDOTDIR isolated), PROMPT shall be set to a non-empty value
    - When `capsule connect` is run and daemon is not running, it shall auto-start the daemon then relay
    - When coproc dies, the next precmd shall attempt reconnection
    - If reconnection fails, the system shall display fallback prompt `%~ %# `
  - Evidence: `run=cargo test -p capsule-core -- init && cargo test -p capsule-cli; oracle=all tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=zsh integration test uses ZDOTDIR/HOME isolation in temp dir`
  - Gates: `static`, `integration`, `system`
  - Executable doc: `ZDOTDIR=$tmp HOME=$tmp zsh -c 'eval "$(capsule init zsh)" && [[ -n "$PROMPT" ]]'` — temp dir 隔離下で PROMPT が設定されることを検証
  - Why not split vertically further?: init/connect/zsh-glue は 1 つのユーザーフローを構成。connect なしに init の出力は検証不能
  - Escalate if: macOS 同梱 zsh の coproc fd 取得方法が想定と異なる (Open Question #1)

- [x] Theme 7: E2E Integration + Observability
  - Outcome: binary build → daemon 起動 → client 接続 → request/response → shutdown の全フローが動作し、構造化ログが出力される
  - Goal: workspace-level E2E テスト、tracing-subscriber によるログ出力 (`$TMPDIR/capsule.log`、`CAPSULE_LOG` でレベル制御)、error recovery の検証
  - Must Not Break: `task check`、既存の全テスト
  - Non-goals: パフォーマンス最適化 (latency 計測・記録のみ)、設定ファイル
  - Acceptance (EARS):
    - When E2E test runs the full flow (daemon start → connect → request → response → cwd change → re-request → response change → shutdown → cleanup), the system shall succeed
    - When `CAPSULE_LOG=debug` is set, the daemon shall output structured log lines to `$TMPDIR/capsule.log`
    - When cwd changes between requests, the response shall reflect the new directory
    - When daemon is stopped and restarted, stale socket shall be detected and removed automatically
  - Evidence: `run=cargo test --workspace -- e2e; oracle=all e2e tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`, `system`
  - Executable doc: `cargo test --workspace -- e2e` — binary build → temp socket daemon → connect → request → RenderResult 検証 → cwd 変更 → 再 request → response 変化確認 → shutdown → socket cleanup
  - Why not split vertically further?: E2E とログ出力は最終統合の 1 スライス。ログなしに E2E のデバッグが困難
  - Escalate if: fast-only latency が 5ms 目標を大幅に超過し、アーキテクチャ変更が必要な場合

- [x] Theme 8: Starship-compatible prompt rendering
  - Outcome: prompt が Starship 互換の見た目で表示される (connector words, Nerd Font icons, left-aligned layout, bracket git status)
  - Goal:
    - render/ に Segment / Connector / Icon 型を導入し、compose を Segment ベースの left-aligned 結合に変更 (right-alignment 廃止)
    - git: branch + bracket 内 status indicators (カウントなし、red bold)
    - directory: git repo 内 → repo-relative パス、repo root → フォルダ名、repo 外 → 現行 `~/...`
    - toolchain: Nerd Font icon + 名前 (バージョンなし)
    - time: line 2 に移動、"at" connector 付き
    - character: 常に `❯`、成功時 green / 失敗時 red (✗ 廃止)
    - status module: compose から除外 (exit code 数値は非表示)
    - cmd_duration: line 1 末尾、connector なし、yellow 維持
    - compose_prompt: Segment ベースの組み立てに変更
  - Must Not Break: `task check`, daemon 統合, wire protocol
  - Non-goals: TOML 設定, toolchain バージョン検出, カスタムテーマ, module trait 変更
  - Acceptance (EARS):
    - When in a git repo at root, line 1 shall start with repo folder name followed by "on" connector, git icon, branch, and bracketed status
    - When in a git repo subdirectory, directory shall show repo-relative path
    - When toolchain is detected, line 1 shall include "via" connector with Nerd Font icon and toolchain name
    - When composing line 2, it shall show "at" connector, time in HH:MM format, then `❯` character
    - When exit code is non-zero, `❯` shall be styled red; when zero, green
    - When no git repo exists, "on" section shall be omitted
    - When no toolchain detected, "via" section shall be omitted
    - When total width exceeds terminal columns, directory shall be truncated first, then rightmost segments dropped
    - When cmd_duration exceeds threshold, it shall appear at the end of line 1 in yellow
  - Evidence: `run=cargo test -p capsule-core; oracle=all render/compose/daemon tests pass with new format; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- render` + `cargo test -p capsule-core -- daemon::tests::compose`
  - Why not split vertically further?: Segment 型、connectors、format 変更は 1 つの visual redesign を構成。部分変更は旧形式でも新形式でもない不整合な見た目を生む
  - Escalate if: git repo root 検出が fast path に >1ms の latency を追加する、または Nerd Font glyph の width 計算で layout がずれる

- [x] Theme 9: Toolchain version detection + Elixir removal
  - Outcome: toolchain セグメントが Starship 互換のアイコン+バージョン+テーマカラーで表示され、Elixir サポートが除去される
  - Goal:
    - ToolchainModule を Slow に変更し、名前検出+バージョン検出を一括実行
    - `ToolchainInfo { name: &'static str, version: String }` を返す `detect()` 関数を追加。バージョン取得失敗時は None を返す（セグメント非表示）
    - 6 toolchain (rust, node, go, python, ruby, bun) のバージョン検出
    - Node: `.node-version` ファイル優先、fallback `node --version`
    - per-toolchain Starship 互換カラー (rust=bold red, node=bold green, go=bold cyan, python=bold yellow, ruby=bold red, bun=bold red)
    - compose_prompt: `via <icon> <version>` 形式（名前テキストなし、アイコンが識別子）
    - Elixir を TOOLCHAIN_FILES、toolchain_icon、README から除去
  - Must Not Break: `task check`, fast prompt の即時表示（toolchain は slow 完了後に Update で出現）
  - Non-goals: バージョンフォーマットのカスタマイズ, タイムアウト設定, 新規 toolchain 追加, Module trait 変更
  - Acceptance (EARS):
    - When slow modules complete and `rustc --version` succeeds, compose_prompt shall render `via <rust-icon> vX.Y.Z` in bold red
    - When slow modules complete and `.node-version` contains a parseable version, compose_prompt shall use that version instead of running `node --version`
    - When a version command fails or the binary is not found, detect() shall return None and the toolchain segment shall be omitted
    - When mix.exs exists in cwd, toolchain detection shall return None
    - When cached slow output contains toolchain info, the initial RenderResult shall include the toolchain segment
  - Evidence: `run=cargo test -p capsule-core; oracle=all module/daemon tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- toolchain` + `cargo test -p capsule-core -- daemon::tests::compose`
  - Why not split vertically further?: Elixir 除去、バージョン追加、カラー変更は同一セグメントの visual redesign。部分適用は不整合な見た目を生む
  - Escalate if: バージョンコマンドの出力フォーマットが OS/バージョンにより大きく異なりパースが安定しない場合

- [x] Theme 10: Starship-compatible git indicators, branch icon, and cmd_duration format
  - Outcome: git status 表示が Starship デフォルトと同等の記号・順序になり、branch アイコンがユーザー指定の Nerd Font に変わり、cmd_duration に `took` 接頭辞が付く
  - Goal:
    - git status indicators を Starship デフォルトに揃える:
      - conflict: `~` → `=`
      - stash: 新規追加 `$` (`--show-stash` フラグ、`GitStatus.stashed` フィールド、`# stash <count>` パース)
      - deleted: 新規追加 `✘` (XY フィールドの `D` を個別追跡)
      - renamed: 新規追加 `»` (`2 ` 行を個別追跡)
      - diverged: ahead かつ behind の場合 `⇡⇣` → `⇕` に統合
    - indicator 表示順を Starship デフォルトに合わせる: `= $ ✘ » ! + ? ⇕ / ⇡⇣`
    - branch icon: `\u{e0a0}` → `\u{f418}` (nf-oct-git_branch)
    - cmd_duration: compose_prompt で connector `"took"` を追加
  - Must Not Break: `task check`, daemon 統合, wire protocol, 既存の git/cmd_duration テスト (期待値更新は許容)
  - Non-goals: deleted/renamed のカウント表示、git_status の format カスタマイズ、stash 詳細表示、cmd_duration threshold 変更
  - Acceptance (EARS):
    - When a git repo has conflicted files, the indicator shall show `=` (not `~`)
    - When a git repo has stashed entries, the indicator shall show `$`
    - When a git repo has deleted files, the indicator shall show `✘`
    - When a git repo has renamed files, the indicator shall show `»`
    - When branch is both ahead and behind upstream, the indicator shall show `⇕` (not `⇡⇣`)
    - When multiple indicators are present, they shall appear in order `= $ ✘ » ! + ? ⇕ / ⇡ ⇣`
    - When git segment is composed, the branch icon shall be `\u{f418}` styled magenta
    - When cmd_duration exceeds threshold, it shall render as `took <duration>` with `took` as connector
  - Evidence: `run=cargo test -p capsule-core; oracle=all module/render/daemon tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- git` + `cargo test -p capsule-core -- cmd_duration` + `cargo test -p capsule-core -- daemon::tests::compose`
  - Why not split vertically further?: git indicator 変更・順序変更・icon 変更・cmd_duration format は 1 つの Starship 互換性テーマを構成。部分適用は旧形式と新形式が混在する不整合を生む
  - Escalate if: `--show-stash` が古い git バージョンで未対応の場合、または deleted/renamed の個別追跡が staged/modified カウントと矛盾する場合

- [x] Theme 11: Directory readonly indicator
  - Outcome: readonly ディレクトリで Starship 互換の lock アイコンがパスの後に赤で表示される
  - Goal:
    - `FastOutputs` に `read_only: bool` フィールド追加
    - `run_fast_modules` で `std::fs::metadata(cwd).permissions().readonly()` により検出
    - `compose_prompt` の directory セグメント構築時に `read_only == true` なら lock アイコン (`\u{f023}`, nf-fa-lock) を赤スタイルでパスの後に付加（セグメントを pre-styled に変更）
  - Must Not Break: `task check`, daemon 統合, wire protocol, 既存の directory/compose テスト (期待値更新は許容)
  - Non-goals: ACL/POSIX capability ベースの権限チェック、カスタムアイコン設定、readonly 時のパス表示変更
  - Acceptance (EARS):
    - When cwd is readonly (no write permission bits), the directory segment shall show lock icon `\u{f023}` in red after the path
    - When cwd is writable, no lock icon shall appear
    - When cwd metadata cannot be read, no lock icon shall appear (fail-open)
  - Evidence: `run=cargo test -p capsule-core; oracle=all module/daemon tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=tempdir with permissions set to readonly`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- directory` + `cargo test -p capsule-core -- daemon::tests::compose`
  - Why not split vertically further?: FastOutputs フィールド追加・検出・表示は 1 つの readonly 機能を構成。部分適用では機能が成立しない
  - Escalate if: macOS の特殊なファイルシステム (firmlinks, synthetic mounts) で `Permissions::readonly()` が予期しない値を返す場合

- [ ] Theme 12: capsule connect relay reconnection
  - Outcome: daemon が再起動しても capsule connect が自動再接続し、shell の prompt が途切れない
  - Goal:
    - relay() を retry loop 化し、socket 側 EOF (daemon 死亡) では再接続、stdin 側 EOF (shell 終了) では正常終了する
    - 再接続時は ensure_daemon() を経由し、daemon が未起動なら自動起動を試みる
    - 再接続上限またはタイムアウトで exit (無限 retry しない)
  - Must Not Break: `task check`, daemon 統合, 既存 e2e テスト
  - Non-goals: disown の done 通知修正 (relay が死ななくなるため通知自体が出なくなる), launchd 統合, zsh glue 変更
  - Acceptance (EARS):
    - When daemon is killed during an active relay, capsule connect shall reconnect within 1s and resume relaying
    - When shell closes stdin pipe, capsule connect shall exit with status 0
    - When reconnection fails after timeout, capsule connect shall exit (precmd が次回再起動)
  - Evidence: `run=cargo test --workspace -- e2e; oracle=reconnection test passes; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=e2e test that kills daemon mid-relay and verifies reconnection`
  - Gates: `static`, `integration`, `system`
  - Executable doc: e2e test: daemon 起動 → connect relay 確立 → daemon kill → relay 再接続 → request/response 成功
  - Why not split vertically further?: stdin EOF 判定と retry loop は不可分。片方だけでは relay の正しさを検証できない
  - Escalate if: tokio::io::copy の完了理由 (EOF vs error) の判別が信頼できない場合

- [ ] Theme 13: launchd daemon management
  - Outcome: daemon が launchd の socket activation で管理され、capsule daemon install/uninstall で plist のライフサイクルが完結する
  - Goal:
    - Server::run が UnixListener を引数で受け取る形にリファクタ (DI)
    - ListenerSource enum (Bind(PathBuf) / Launchd(String) / Systemd) で listener 取得を抽象化。acquire_listener(source) → UnixListener で、プラットフォーム依存コード (#[cfg(target_os)]) はこの関数の数行に閉じる
    - Launchd: launch_activate_socket の raw FFI で fd 取得。Systemd: $LISTEN_FDS から fd 3 取得。Bind: 従来の bind。今回実装するのは Bind + Launchd のみ (Systemd は variant 定義のみ、実装は将来)
    - launch_activate_socket の raw FFI で launchd からソケット fd を取得
    - ソケットパスを $TMPDIR/capsule.sock → ~/.capsule/capsule.sock に変更
    - launchd モード時は inode check を無効化
    - launchd モード時は flock による排他制御を無効化
    - capsule daemon が自動検出で launchd / standalone を切り替え
    - capsule daemon install: plist 生成 → ~/Library/LaunchAgents/ に配置 → launchctl bootstrap。冪等 (差分なければ skip、あれば bootout → bootstrap)
    - capsule daemon uninstall: launchctl bootout → plist 削除
    - docs/architecture.md の Key Tech Decisions 更新
    - ADR: docs/adr/launchd-daemon-management.md
  - Must Not Break: `task check`, 既存テスト (TestHarness は Bind モードで動作), capsule connect の互換性
  - Non-goals: systemd 実装 (ListenerSource::Systemd variant は定義するが acquire_listener の中身は未実装), zsh glue の auto-start 除去 (fallback として維持), plist のカスタマイズオプション
  - Acceptance (EARS):
    - When daemon runs under launchd with socket activation, Server shall receive listener from launch_activate_socket and serve requests
    - When daemon runs without launchd, Server shall bind to ~/.capsule/capsule.sock (standalone モード)
    - When TestHarness creates a listener and passes it to Server, tests shall pass without sleep
    - When running under launchd, inode check and flock shall be skipped
    - When `capsule daemon install` is run, plist shall be written to ~/Library/LaunchAgents/ and loaded via launchctl bootstrap
    - When `capsule daemon install` is run and plist is already current, no reload shall occur
    - When `capsule daemon install` is run and plist differs, the service shall be reloaded with the new plist
    - When `capsule daemon uninstall` is run, the service shall be stopped and plist removed
  - Evidence: `run=cargo test --workspace; oracle=all tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=launchd 統合は手動検証 (CI に launchd なし); unit test で launch_activate_socket の成功/失敗分岐と install/uninstall のファイル操作を検証`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- daemon` + `cargo test -p capsule-cli -- daemon` (Bind モードで全既存テスト通過) + 手動: `capsule daemon install` → launchd 経由で daemon 起動確認 → `capsule daemon uninstall` → 停止・plist 削除確認
  - Why not split vertically further?: Server DI・listener 抽象化・install/uninstall・inode check/flock 条件分岐は launchd 統合の構成要素。DI だけ、または install だけでは外部観測可能な前進がない
  - Escalate if: launch_activate_socket が Unix domain socket で期待通り動作しない場合、launchd の socket activation が capsule の coproc relay モデルと両立しない場合、または sun_path 制限で ~/.capsule/capsule.sock が使えない場合

- [ ] Theme 14: Config file infrastructure + built-in module customization
  - Outcome: ユーザーが `$XDG_CONFIG_HOME/capsule/config.toml` で既存モジュールの色・アイコン・閾値・有効/無効を設定でき、config なしでは現在の動作と同一
  - Goal:
    - serde, toml 依存の追加 (v2 依存として解禁)
    - Config struct (全フィールドに Default 実装、現在のハードコード値と同一)
    - Config パス解決: $XDG_CONFIG_HOME/capsule/config.toml (fallback ~/.capsule/config.toml)
    - daemon 起動時に config 読み込み、Arc<Config> で Server に保持
    - compose_prompt, run_fast_modules, toolchain_icon, toolchain_style が &Config を参照
    - 設定可能項目: character (glyph, success_color, error_color), directory (color), git (icon, indicator 色), time (format, color, enabled), cmd_duration (threshold_ms, color), connector words
    - config file が存在しない場合は compiled-in defaults (現在の動作と同一)
    - config file に構文エラーがある場合は log error + defaults 使用
    - docs/architecture.md の Key Tech Decisions 更新
  - Must Not Break: `task check`, config なしでの既存動作 (全テスト既存期待値そのまま), daemon 統合, wire protocol
  - Non-goals: toolchain DSL (Theme 15), env var segments, hot-reload (restart required), module ordering/layout customization, config file テンプレート同梱
  - Acceptance (EARS):
    - When no config file exists, daemon shall behave identically to current hardcoded defaults
    - When config file sets character.glyph = "$", the prompt character shall change to "$"
    - When config file sets cmd_duration.threshold_ms = 5000, durations below 5000ms shall not be shown
    - When config file sets time.enabled = false, time segment shall be omitted from line 2
    - When config file has syntax errors, daemon shall log error and use compiled-in defaults
  - Evidence: `run=cargo test --workspace; oracle=all tests pass including new config tests; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=config tests use tempfile with custom config.toml`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- config` + `cargo test -p capsule-core -- daemon::tests::compose` (config 有無で既存テスト通過 + 新規 config override テスト)
  - Why not split vertically further?: Config loading infrastructure と built-in module customization は一体。infra だけでは外部観測可能な前進がなく、customization だけでは config loading がない
  - Escalate if: serde の derive マクロが workspace lints (deny unwrap/expect) と衝突する場合、または Config struct の Default 実装で全ハードコード値の再現が困難な場合

- [ ] Theme 15: User-defined toolchain via config DSL
  - Outcome: ユーザーが TOML の [[toolchain]] セクションで任意の言語ツールチェインを定義し、regex でバージョンを抽出でき、prompt に表示される
  - Goal:
    - regex-lite 依存の追加
    - ToolchainDef struct (name, files, version_file, command, version_regex, icon, color)
    - Config の [[toolchain]] セクションのパース、daemon 起動時に regex コンパイル
    - detect_toolchain と detect_version を ToolchainDef ベースのデータ駆動に変更
    - デフォルト toolchain 定義 (現在の 6 toolchain) を compiled-in で維持
    - version_regex の最初のキャプチャグループがバージョン文字列 (自動 v prefix 付加)
  - Must Not Break: `task check`, config なしでの既存 toolchain 動作 (全テスト既存期待値そのまま), daemon 統合
  - Non-goals: env var segments, file read segments, toolchain タイムアウト設定, module ordering 変更
  - Acceptance (EARS):
    - When no [[toolchain]] config exists, built-in 6 toolchains shall work identically to current behavior
    - When [[toolchain]] with name="zig", files=["build.zig"], command=["zig","version"] is configured and build.zig exists, the prompt shall show zig version
    - When version_regex captures a group from command output, the captured text shall be used as version with v prefix
    - When version command fails or binary is not found, the toolchain segment shall be omitted
  - Evidence: `run=cargo test -p capsule-core -- toolchain; oracle=all tests pass; visibility=independent; controls=[context]; missing=[agent]; companion=none; notes=custom toolchain tests use tempfile with config + marker file + mock command output`
  - Gates: `static`, `integration`
  - Executable doc: `cargo test -p capsule-core -- toolchain` (既存 6 toolchain テスト通過 + 新規 custom toolchain テスト)
  - Why not split vertically further?: regex 導入・ToolchainDef 構造・データ駆動化は 1 つの DSL 機能を構成。部分適用では custom toolchain が動作しない
  - Escalate if: regex-lite が必要なパターンをカバーできない場合、またはデフォルト toolchain と user-defined toolchain の merge/override 方針 (Open Question) が解決できない場合
